const sum = (firstPaeam, secondParam) => {
    return firstPaeam + secondParam;
};
console.log(sum(2, 5));

const printHello = () => {
    console.log("hello");
};
printHello();

const checkWeigth = (weigth) => {
    console.log(`Baggage weigth : ${weigth} kilograms.`);
};
checkWeigth(25);

const multply = (a, b) => a * b;
console.log(multply(2, 30));